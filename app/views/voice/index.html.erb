<!DOCTYPE html>
<html>
<head>
  <title>Voice Recorder</title>
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <style>
    :root {
      --primary: #4361ee;
      --primary-light: #6c8aff;
      --primary-dark: #3a56d4;
      --secondary: #7209b7;
      --accent: #4cc9f0;
      --danger: #f72585;
      --warning: #ff9e00;
      --success: #06d6a0;
      --light: #f8f9fc;
      --dark: #2b2d42;
      --gray: #8d99ae;
      --border-radius: 16px;
      --shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      --transition: all 0.4s ease;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', 'Segoe UI', sans-serif;
      color: var(--dark);
      background: linear-gradient(135deg, #f5f7ff 0%, #f0f4ff 100%);
      min-height: 100vh;
      line-height: 1.6;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .voice-recorder-container {
      max-width: 600px;
      width: 100%;
    }

    /* Header Card */
    .header-card {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      border-radius: var(--border-radius);
      padding: 2rem;
      margin-bottom: 2rem;
      box-shadow: var(--shadow);
      border: 1px solid rgba(255, 255, 255, 0.8);
      text-align: center;
    }

    .header-content h1 {
      font-weight: 800;
      font-size: 2.2rem;
      margin-bottom: 0.5rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .header-content p {
      color: var(--gray);
      font-size: 1.1rem;
    }

    /* Main Recorder Card */
    .recorder-card {
      background: white;
      border-radius: var(--border-radius);
      padding: 2.5rem;
      box-shadow: var(--shadow);
      transition: var(--transition);
      border-top: 4px solid var(--primary);
    }

    .recorder-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
    }

    /* Status Indicator */
    .status-indicator {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1rem;
      margin-bottom: 2rem;
      padding: 1rem;
      background: var(--light);
      border-radius: var(--border-radius);
      border-left: 4px solid var(--primary-light);
    }

    .status-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: var(--gray);
      transition: var(--transition);
    }

    .status-dot.recording {
      background: var(--danger);
      animation: pulse 2s infinite;
    }

    .status-text {
      font-weight: 600;
      color: var(--dark);
    }

    @keyframes pulse {
      0% { transform: scale(1); opacity: 1; }
      50% { transform: scale(1.2); opacity: 0.7; }
      100% { transform: scale(1); opacity: 1; }
    }

    /* Controls Section */
    .controls-section {
      display: flex;
      justify-content: center;
      gap: 1.5rem;
      margin-bottom: 2rem;
      flex-wrap: wrap;
    }

    .control-btn {
      padding: 1rem 2rem;
      border: none;
      border-radius: 50px;
      font-weight: 600;
      cursor: pointer;
      transition: var(--transition);
      display: flex;
      align-items: center;
      gap: 0.8rem;
      font-size: 1rem;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
      position: relative;
      overflow: hidden;
    }

    .control-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
      transition: var(--transition);
    }

    .control-btn:hover::before {
      left: 100%;
    }

    .control-btn:disabled {
      opacity: 0.6;
      cursor: not-allowed;
      transform: none !important;
      box-shadow: none !important;
    }

    .control-btn:disabled:hover::before {
      left: -100%;
    }

    .start-btn {
      background: linear-gradient(135deg, var(--success) 0%, #05c291 100%);
      color: white;
    }

    .start-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(6, 214, 160, 0.4);
    }

    .stop-btn {
      background: linear-gradient(135deg, var(--danger) 0%, #f55f7c 100%);
      color: white;
    }

    .stop-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(247, 37, 133, 0.4);
    }

    /* Output Section */
    .output-section {
      margin-bottom: 2rem;
    }

    .output-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1rem;
    }

    .output-header h2 {
      font-weight: 700;
      font-size: 1.2rem;
      color: var(--dark);
    }

    .word-count {
      background: var(--primary-light);
      color: white;
      padding: 0.3rem 0.8rem;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
    }

    .output-textarea {
      width: 100%;
      min-height: 200px;
      padding: 1.5rem;
      border: 2px solid var(--primary-light);
      border-radius: var(--border-radius);
      background: white;
      color: var(--dark);
      font-size: 1rem;
      line-height: 1.6;
      resize: vertical;
      transition: var(--transition);
      font-family: 'Inter', sans-serif;
    }

    .output-textarea:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.1);
    }

    .output-textarea::placeholder {
      color: var(--gray);
    }

    /* Action Buttons */
    .action-buttons {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .action-btn {
      padding: 0.8rem 1.5rem;
      border: 2px solid;
      border-radius: 50px;
      font-weight: 600;
      cursor: pointer;
      transition: var(--transition);
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.9rem;
    }

    .copy-btn {
      background: rgba(67, 97, 238, 0.1);
      border-color: var(--primary);
      color: var(--primary);
    }

    .copy-btn:hover {
      background: var(--primary);
      color: white;
    }

    .clear-btn {
      background: rgba(247, 37, 133, 0.1);
      border-color: var(--danger);
      color: var(--danger);
    }

    .clear-btn:hover {
      background: var(--danger);
      color: white;
    }

    /* Browser Support Alert */
    .browser-alert {
      background: rgba(255, 158, 0, 0.1);
      border: 1px solid rgba(255, 158, 0, 0.3);
      border-left: 4px solid var(--warning);
      color: var(--dark);
      padding: 1rem 1.5rem;
      border-radius: var(--border-radius);
      margin-top: 2rem;
      font-size: 0.9rem;
    }

    .browser-alert.hidden {
      display: none;
    }

    .alert-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.5rem;
      font-weight: 600;
      color: var(--warning);
    }

    /* Success Message */
    .success-message {
      position: fixed;
      top: 2rem;
      right: 2rem;
      background: var(--success);
      color: white;
      padding: 1rem 1.5rem;
      border-radius: var(--border-radius);
      box-shadow: var(--shadow);
      display: flex;
      align-items: center;
      gap: 0.5rem;
      transform: translateX(400px);
      transition: transform 0.4s ease;
      z-index: 1000;
    }

    .success-message.show {
      transform: translateX(0);
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      body {
        padding: 1rem;
      }

      .header-card {
        padding: 1.5rem;
      }

      .header-content h1 {
        font-size: 1.8rem;
      }

      .recorder-card {
        padding: 2rem;
      }

      .controls-section {
        flex-direction: column;
        align-items: center;
      }

      .control-btn {
        width: 100%;
        justify-content: center;
      }

      .action-buttons {
        flex-direction: column;
      }

      .action-btn {
        width: 100%;
        justify-content: center;
      }

      .success-message {
        top: 1rem;
        right: 1rem;
        left: 1rem;
        transform: translateY(-100px);
      }

      .success-message.show {
        transform: translateY(0);
      }
    }

    @media (max-width: 480px) {
      .header-content h1 {
        font-size: 1.5rem;
      }

      .recorder-card {
        padding: 1.5rem;
      }

      .output-textarea {
        min-height: 150px;
        padding: 1rem;
      }
    }
  </style>
</head>

<body>
  <div class="voice-recorder-container">
    <!-- Header -->
    <header class="header-card">
      <div class="header-content">
        <h1>Voice to Text Recorder</h1>
        <p>Convert your speech to text using advanced Web Speech API</p>
      </div>
    </header>

    <!-- Main Recorder -->
    <main class="recorder-card">
      <!-- Status Indicator -->
      <div class="status-indicator">
        <div class="status-dot" id="statusDot"></div>
        <div class="status-text" id="statusText">Ready to record</div>
      </div>

      <!-- Controls -->
      <div class="controls-section">
        <button id="startBtn" class="control-btn start-btn">
          <i class="fas fa-microphone"></i>
          Start Recording
        </button>

        <button id="stopBtn" class="control-btn stop-btn" disabled>
          <i class="fas fa-stop"></i>
          Stop Recording
        </button>
      </div>

      <!-- Output Section -->
      <section class="output-section">
        <div class="output-header">
          <h2>Converted Text</h2>
          <div class="word-count" id="wordCount">0 words</div>
        </div>
        
        <textarea id="outputText"
                  class="output-textarea"
                  placeholder="Your speech will appear here... Speak clearly and naturally."></textarea>
      </section>

      <!-- Action Buttons -->
      <div class="action-buttons">
        <button id="copyBtn" class="action-btn copy-btn">
          <i class="fas fa-copy"></i>
          Copy Text
        </button>

        <button id="clearBtn" class="action-btn clear-btn">
          <i class="fas fa-broom"></i>
          Clear Text
        </button>
      </div>
    </main>

    <!-- Browser Support Alert -->
    <div id="browserAlert" class="browser-alert hidden">
      <div class="alert-header">
        <i class="fas fa-info-circle"></i>
        Browser Compatibility
      </div>
      <p>For best results, use Chrome, Edge, or Safari. Make sure your microphone is enabled and you're in a quiet environment.</p>
    </div>
  </div>

  <!-- Success Message -->
  <div id="successMessage" class="success-message">
    <i class="fas fa-check-circle"></i>
    <span id="successText">Text copied to clipboard!</span>
  </div>

  <script>
    let recognition;
    let isRecording = false;

    // DOM Elements
    const startBtn = document.getElementById('startBtn');
    const stopBtn = document.getElementById('stopBtn');
    const copyBtn = document.getElementById('copyBtn');
    const clearBtn = document.getElementById('clearBtn');
    const outputText = document.getElementById('outputText');
    const statusDot = document.getElementById('statusDot');
    const statusText = document.getElementById('statusText');
    const wordCount = document.getElementById('wordCount');
    const browserAlert = document.getElementById('browserAlert');
    const successMessage = document.getElementById('successMessage');
    const successText = document.getElementById('successText');

    // Initialize the app
    function initApp() {
      // Check browser support
      if (!("webkitSpeechRecognition" in window) && !("SpeechRecognition" in window)) {
        showBrowserAlert("Your browser does not support Speech Recognition. Please use Chrome, Edge, or Safari for the best experience.");
        disableControls();
        return;
      }

      // Show browser tips for supported browsers
      showBrowserAlert("For best results, use Chrome, Edge, or Safari. Make sure your microphone is enabled and you're in a quiet environment.");

      // Initialize speech recognition
      const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
      recognition = new SpeechRecognition();

      recognition.continuous = true;
      recognition.interimResults = true;
      recognition.lang = "en-US";

      recognition.onresult = function(event) {
        let transcript = "";
        for (let i = event.resultIndex; i < event.results.length; i++) {
          if (event.results[i].isFinal) {
            transcript += event.results[i][0].transcript;
          } else {
            transcript += event.results[i][0].transcript;
          }
        }
        outputText.value = transcript;
        updateWordCount();
      };

      recognition.onstart = function() {
        updateStatus('recording', 'Recording in progress...');
      };

      recognition.onend = function() {
        if (isRecording) {
          // If recording was stopped unexpectedly, try to restart
          try {
            recognition.start();
          } catch (e) {
            stopRecording();
          }
        } else {
          updateStatus('ready', 'Recording stopped');
        }
      };

      recognition.onerror = function(event) {
        console.error("Speech recognition error:", event.error);
        if (event.error === 'not-allowed') {
          showError('Microphone access denied. Please allow microphone permissions.');
        } else if (event.error === 'audio-capture') {
          showError('No microphone found. Please check your audio device.');
        } else {
          showError('Speech recognition error: ' + event.error);
        }
        stopRecording();
      };
    }

    // Control functions
    function startRecording() {
      if (recognition && !isRecording) {
        try {
          recognition.start();
          isRecording = true;
          startBtn.disabled = true;
          stopBtn.disabled = false;
          updateStatus('recording', 'Recording in progress...');
        } catch (e) {
          showError('Failed to start recording: ' + e.message);
        }
      }
    }

    function stopRecording() {
      if (recognition && isRecording) {
        recognition.stop();
        isRecording = false;
        startBtn.disabled = false;
        stopBtn.disabled = true;
        updateStatus('ready', 'Recording completed');
      }
    }

    function copyToClipboard() {
      if (outputText.value.trim() === '') {
        showSuccess('No text to copy');
        return;
      }

      outputText.select();
      outputText.setSelectionRange(0, 99999); // For mobile devices

      try {
        navigator.clipboard.writeText(outputText.value).then(() => {
          showSuccess('Text copied to clipboard!');
        }).catch(() => {
          // Fallback for older browsers
          document.execCommand('copy');
          showSuccess('Text copied to clipboard!');
        });
      } catch (e) {
        showSuccess('Text copied to clipboard!');
      }
    }

    function clearText() {
      if (outputText.value.trim() === '') {
        showSuccess('Text area is already empty');
        return;
      }

      if (confirm('Are you sure you want to clear all text?')) {
        outputText.value = '';
        updateWordCount();
        showSuccess('Text cleared successfully');
      }
    }

    // UI Helper functions
    function updateStatus(status, message) {
      statusDot.className = 'status-dot';
      if (status === 'recording') {
        statusDot.classList.add('recording');
      }
      statusText.textContent = message;
    }

    function updateWordCount() {
      const text = outputText.value.trim();
      const words = text === '' ? 0 : text.split(/\s+/).length;
      wordCount.textContent = `${words} word${words !== 1 ? 's' : ''}`;
    }

    function showBrowserAlert(message) {
      browserAlert.querySelector('p').textContent = message;
      browserAlert.classList.remove('hidden');
    }

    function showError(message) {
      successText.textContent = message;
      successMessage.style.background = 'var(--danger)';
      successMessage.classList.add('show');
      setTimeout(() => {
        successMessage.classList.remove('show');
      }, 4000);
    }

    function showSuccess(message) {
      successText.textContent = message;
      successMessage.style.background = 'var(--success)';
      successMessage.classList.add('show');
      setTimeout(() => {
        successMessage.classList.remove('show');
      }, 3000);
    }

    function disableControls() {
      startBtn.disabled = true;
      stopBtn.disabled = true;
      copyBtn.disabled = true;
      clearBtn.disabled = true;
    }

    // Event Listeners
    startBtn.addEventListener('click', startRecording);
    stopBtn.addEventListener('click', stopRecording);
    copyBtn.addEventListener('click', copyToClipboard);
    clearBtn.addEventListener('click', clearText);

    // Update word count when typing manually
    outputText.addEventListener('input', updateWordCount);

    // Initialize the application
    document.addEventListener('DOMContentLoaded', initApp);
  </script>
</body>
</html>