<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OG - Futuristic Data Platform</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Exo+2:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --primary: #00f5ff;
      --primary-glow: #00f5ff66;
      --primary-dark: #00a8cc;
      --secondary: #9d4edd;
      --secondary-glow: #9d4edd66;
      --accent: #ff006e;
      --accent-glow: #ff006e66;
      --success: #38b000;
      --dark: #0c0f1d;
      --darker: #070a15;
      --light: #e2fafc;
      --gray: #8d99ae;
      --border-radius: 12px;
      --transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Exo 2', sans-serif;
      color: var(--light);
      background: var(--darker);
      overflow-x: hidden;
      line-height: 1.6;
    }

    h1, h2, h3, h4, h5, .futuristic {
      font-family: 'Orbitron', sans-serif;
      font-weight: 700;
    }

    /* Loading Animation */
    #loading-screen {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: var(--darker);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      transition: opacity 0.8s ease, visibility 0.8s ease;
    }

    .loading-content {
      text-align: center;
      position: relative;
    }

    .logo-loader {
      font-size: 4rem;
      margin-bottom: 2rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 0 0 20px var(--primary-glow);
      animation: pulse 2s infinite alternate;
    }

    .loading-bar-container {
      width: 300px;
      height: 6px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 10px;
      overflow: hidden;
      margin: 2rem auto;
      position: relative;
    }

    .loading-bar {
      height: 100%;
      width: 0%;
      background: linear-gradient(90deg, var(--primary) 0%, var(--secondary) 100%);
      border-radius: 10px;
      transition: width 0.3s ease;
      box-shadow: 0 0 10px var(--primary-glow);
    }

    .loading-dots {
      display: flex;
      justify-content: center;
      margin-top: 1rem;
    }

    .dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: var(--primary);
      margin: 0 5px;
      opacity: 0.6;
      animation: dotPulse 1.5s infinite ease-in-out;
    }

    .dot:nth-child(2) {
      animation-delay: 0.2s;
    }

    .dot:nth-child(3) {
      animation-delay: 0.4s;
    }

    @keyframes pulse {
      0% { transform: scale(1); text-shadow: 0 0 10px var(--primary-glow); }
      100% { transform: scale(1.05); text-shadow: 0 0 25px var(--primary-glow), 0 0 35px var(--secondary-glow); }
    }

    @keyframes dotPulse {
      0%, 100% { transform: scale(1); opacity: 0.6; }
      50% { transform: scale(1.5); opacity: 1; }
    }

    /* Main Content */
    #main-content {
      opacity: 0;
      transition: opacity 0.8s ease;
    }

    /* Header & Navigation */
    .navbar {
      background: rgba(12, 15, 29, 0.8);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0, 245, 255, 0.2);
      padding: 1rem 0;
      transition: var(--transition);
    }

    .navbar-brand {
      font-family: 'Orbitron', sans-serif;
      font-weight: 900;
      font-size: 1.8rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      display: flex;
      align-items: center;
      text-shadow: 0 0 15px var(--primary-glow);
    }

    .navbar-brand i {
      margin-right: 0.5rem;
      font-size: 2rem;
    }

    .nav-link {
      font-weight: 500;
      color: var(--light);
      margin: 0 0.5rem;
      transition: var(--transition);
      position: relative;
    }

    .nav-link:hover {
      color: var(--primary);
    }

    .nav-link::after {
      content: '';
      position: absolute;
      bottom: -5px;
      left: 0;
      width: 0;
      height: 2px;
      background: var(--primary);
      transition: var(--transition);
      box-shadow: 0 0 8px var(--primary);
    }

    .nav-link:hover::after {
      width: 100%;
    }

    .btn-login, .btn-signup {
      border-radius: 50px;
      font-weight: 600;
      padding: 0.5rem 1.5rem;
      transition: var(--transition);
      font-family: 'Orbitron', sans-serif;
    }

    .btn-login {
      background: transparent;
      border: 2px solid var(--primary);
      color: var(--primary);
      box-shadow: 0 0 10px var(--primary-glow);
    }

    .btn-login:hover {
      background: var(--primary);
      color: var(--dark);
      transform: translateY(-3px);
      box-shadow: 0 0 20px var(--primary-glow);
    }

    .btn-signup {
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      border: none;
      color: var(--dark);
      margin-left: 0.5rem;
      box-shadow: 0 0 15px var(--primary-glow);
    }

    .btn-signup:hover {
      background: linear-gradient(135deg, var(--primary-dark) 0%, var(--secondary) 100%);
      transform: translateY(-3px);
      box-shadow: 0 0 25px var(--primary-glow);
    }

    /* Hero Section */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      position: relative;
      overflow: hidden;
      padding: 4rem 0;
      background: radial-gradient(ellipse at center, rgba(0, 245, 255, 0.1) 0%, transparent 70%);
    }

    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 20% 80%, rgba(157, 78, 221, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(0, 245, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 40% 40%, rgba(255, 0, 110, 0.05) 0%, transparent 50%);
      z-index: -1;
    }

    .hero-content {
      position: relative;
      z-index: 2;
    }

    .hero h1 {
      font-weight: 900;
      font-size: 4rem;
      line-height: 1.2;
      margin-bottom: 1.5rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 0 0 30px var(--primary-glow);
      animation: textGlow 3s infinite alternate;
    }

    @keyframes textGlow {
      0% { text-shadow: 0 0 20px var(--primary-glow); }
      100% { text-shadow: 0 0 30px var(--primary-glow), 0 0 40px var(--secondary-glow); }
    }

    .hero p {
      font-size: 1.3rem;
      color: var(--light);
      margin-bottom: 2rem;
      max-width: 600px;
      opacity: 0.9;
    }

    .hero-btns .btn {
      border-radius: 50px;
      padding: 0.8rem 2rem;
      font-weight: 600;
      margin-right: 1rem;
      margin-bottom: 1rem;
      transition: var(--transition);
      font-family: 'Orbitron', sans-serif;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
      border: none;
      color: var(--dark);
      box-shadow: 0 0 20px var(--primary-glow);
    }

    .btn-primary:hover {
      transform: translateY(-5px);
      box-shadow: 0 0 30px var(--primary-glow);
    }

    .btn-outline-primary {
      border: 2px solid var(--primary);
      color: var(--primary);
      box-shadow: 0 0 15px var(--primary-glow);
      background: transparent;
    }

    .btn-outline-primary:hover {
      background: var(--primary);
      color: var(--dark);
      transform: translateY(-5px);
      box-shadow: 0 0 25px var(--primary-glow);
    }

    .hero-image {
      position: relative;
      z-index: 1;
    }

    .hologram-container {
      position: relative;
      width: 100%;
      height: 500px;
      perspective: 1000px;
    }

    .hologram {
      position: absolute;
      width: 100%;
      height: 100%;
      transform-style: preserve-3d;
      animation: hologramFloat 8s infinite ease-in-out;
    }

    .hologram-layer {
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: 20px;
      background: linear-gradient(135deg, rgba(0, 245, 255, 0.1) 0%, rgba(157, 78, 221, 0.1) 100%);
      border: 1px solid rgba(0, 245, 255, 0.3);
      box-shadow: 
        inset 0 0 20px rgba(0, 245, 255, 0.2),
        0 0 50px rgba(0, 245, 255, 0.1);
      backdrop-filter: blur(5px);
    }

    .hologram-layer:nth-child(1) {
      transform: translateZ(0px);
    }

    .hologram-layer:nth-child(2) {
      transform: translateZ(30px);
    }

    .hologram-layer:nth-child(3) {
      transform: translateZ(60px);
    }

    .hologram-content {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
      z-index: 10;
      width: 80%;
    }

    @keyframes hologramFloat {
      0%, 100% { transform: rotateY(0deg) rotateX(5deg); }
      50% { transform: rotateY(10deg) rotateX(-5deg); }
    }

    /* Features Section */
    .features {
      padding: 8rem 0;
      background: var(--dark);
      position: relative;
      overflow: hidden;
    }

    .features::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 30% 70%, rgba(0, 245, 255, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 70% 30%, rgba(157, 78, 221, 0.05) 0%, transparent 50%);
      z-index: 0;
    }

    .section-title {
      text-align: center;
      margin-bottom: 5rem;
      position: relative;
      z-index: 1;
    }

    .section-title h2 {
      font-weight: 800;
      font-size: 3rem;
      margin-bottom: 1rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 0 0 20px var(--primary-glow);
    }

    .section-title p {
      color: var(--light);
      font-size: 1.2rem;
      max-width: 600px;
      margin: 0 auto;
      opacity: 0.8;
    }

    .feature-card {
      background: rgba(255, 255, 255, 0.05);
      border-radius: var(--border-radius);
      padding: 2.5rem 2rem;
      transition: var(--transition);
      height: 100%;
      text-align: center;
      border: 1px solid rgba(0, 245, 255, 0.1);
      backdrop-filter: blur(10px);
      position: relative;
      overflow: hidden;
      z-index: 1;
    }

    .feature-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(0, 245, 255, 0.05) 0%, rgba(157, 78, 221, 0.05) 100%);
      z-index: -1;
      opacity: 0;
      transition: var(--transition);
    }

    .feature-card:hover {
      transform: translateY(-10px);
      border-color: rgba(0, 245, 255, 0.3);
      box-shadow: 0 10px 30px rgba(0, 245, 255, 0.1);
    }

    .feature-card:hover::before {
      opacity: 1;
    }

    .feature-icon {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      background: rgba(0, 245, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem;
      border: 1px solid rgba(0, 245, 255, 0.3);
      position: relative;
      overflow: hidden;
    }

    .feature-icon::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      opacity: 0.1;
      z-index: -1;
    }

    .feature-icon i {
      font-size: 2.5rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .feature-card h3 {
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--light);
    }

    .feature-card p {
      color: var(--gray);
    }

    /* How It Works */
    .how-it-works {
      padding: 8rem 0;
      background: var(--darker);
      position: relative;
      overflow: hidden;
    }

    .how-it-works::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 10% 10%, rgba(0, 245, 255, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 90% 90%, rgba(157, 78, 221, 0.05) 0%, transparent 50%);
      z-index: 0;
    }

    .step-card {
      background: rgba(255, 255, 255, 0.05);
      border-radius: var(--border-radius);
      padding: 2.5rem;
      text-align: center;
      position: relative;
      transition: var(--transition);
      border: 1px solid rgba(0, 245, 255, 0.1);
      backdrop-filter: blur(10px);
      height: 100%;
    }

    .step-card:hover {
      transform: translateY(-5px);
      border-color: rgba(0, 245, 255, 0.3);
      box-shadow: 0 10px 25px rgba(0, 245, 255, 0.1);
    }

    .step-number {
      width: 70px;
      height: 70px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      color: var(--dark);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 900;
      font-size: 1.5rem;
      margin: 0 auto 1.5rem;
      box-shadow: 0 0 20px var(--primary-glow);
    }

    /* Payor Analysis */
    .payor-analysis {
      padding: 8rem 0;
      background: var(--dark);
      position: relative;
      overflow: hidden;
    }

    .payor-analysis::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 70% 30%, rgba(0, 245, 255, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 30% 70%, rgba(157, 78, 221, 0.05) 0%, transparent 50%);
      z-index: 0;
    }

    .analysis-card {
      background: rgba(255, 255, 255, 0.05);
      border-radius: var(--border-radius);
      padding: 2.5rem;
      transition: var(--transition);
      height: 100%;
      border: 1px solid rgba(0, 245, 255, 0.1);
      backdrop-filter: blur(10px);
    }

    .analysis-card:hover {
      transform: translateY(-5px);
      border-color: rgba(0, 245, 255, 0.3);
      box-shadow: 0 10px 30px rgba(0, 245, 255, 0.1);
    }

    .analysis-icon {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      background: rgba(0, 245, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1.5rem;
      border: 1px solid rgba(0, 245, 255, 0.3);
    }

    .analysis-icon i {
      font-size: 2rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    /* CTA Section */
    .cta-section {
      padding: 8rem 0;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      color: var(--dark);
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .cta-section::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M11 18c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm48 25c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm-43-7c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm63 31c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM34 90c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm56-76c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM12 86c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm28-65c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm23-11c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-6 60c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm29 22c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zM32 63c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm57-13c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-9-21c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM60 91c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM35 41c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM12 60c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2z' fill='%23000' fill-opacity='0.05' fill-rule='evenodd'/%3E%3C/svg%3E");
      z-index: 0;
    }

    .cta-section h2 {
      font-weight: 900;
      font-size: 3rem;
      margin-bottom: 1.5rem;
      position: relative;
      z-index: 1;
    }

    .cta-section p {
      font-size: 1.3rem;
      margin-bottom: 2rem;
      max-width: 700px;
      margin-left: auto;
      margin-right: auto;
      opacity: 0.9;
      position: relative;
      z-index: 1;
    }

    .btn-light {
      background: var(--dark);
      color: var(--primary);
      border-radius: 50px;
      padding: 0.8rem 2.5rem;
      font-weight: 600;
      transition: var(--transition);
      font-family: 'Orbitron', sans-serif;
      border: none;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
      position: relative;
      z-index: 1;
    }

    .btn-light:hover {
      transform: translateY(-3px);
      box-shadow: 0 0 30px rgba(0, 0, 0, 0.3);
      background: var(--darker);
    }

    /* Footer */
    .footer {
      background: var(--darker);
      color: var(--light);
      padding: 4rem 0 2rem;
      border-top: 1px solid rgba(0, 245, 255, 0.1);
    }

    .footer h5 {
      font-weight: 700;
      margin-bottom: 1.5rem;
      font-family: 'Orbitron', sans-serif;
    }

    .footer ul {
      list-style: none;
      padding: 0;
    }

    .footer ul li {
      margin-bottom: 0.8rem;
    }

    .footer ul li a {
      color: rgba(255, 255, 255, 0.7);
      text-decoration: none;
      transition: var(--transition);
    }

    .footer ul li a:hover {
      color: var(--primary);
      padding-left: 5px;
    }

    .social-icons a {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      color: var(--light);
      margin-right: 0.5rem;
      transition: var(--transition);
      border: 1px solid rgba(0, 245, 255, 0.2);
    }

    .social-icons a:hover {
      background: var(--primary);
      color: var(--dark);
      transform: translateY(-3px);
      box-shadow: 0 0 15px var(--primary-glow);
    }

    .footer-bottom {
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      padding-top: 2rem;
      margin-top: 3rem;
      text-align: center;
      color: rgba(255, 255, 255, 0.6);
    }

    /* Responsive Design */
    @media (max-width: 992px) {
      .hero h1 {
        font-size: 3rem;
      }
      
      .hologram-container {
        height: 400px;
        margin-top: 3rem;
      }
    }

    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2.5rem;
      }
      
      .hero p {
        font-size: 1.1rem;
      }
      
      .section-title h2 {
        font-size: 2.2rem;
      }
      
      .feature-card, .step-card, .analysis-card {
        margin-bottom: 1.5rem;
      }
      
      .btn-login, .btn-signup {
        margin-bottom: 0.5rem;
      }
    }

    @media (max-width: 576px) {
      .hero h1 {
        font-size: 2rem;
      }
      
      .hero-btns .btn {
        display: block;
        width: 100%;
        margin-right: 0;
      }
      
      .logo-loader {
        font-size: 3rem;
      }
    }
  </style>
</head>

<body>
  <!-- Loading Screen -->
  <div id="loading-screen">
    <div class="loading-content">
      <div class="logo-loader">
        <i class="fas fa-cube"></i> OG
      </div>
      <div class="loading-bar-container">
        <div class="loading-bar" id="loading-bar"></div>
      </div>
      <div class="loading-dots">
        <div class="dot"></div>
        <div class="dot"></div>
        <div class="dot"></div>
      </div>
      <p class="mt-3">Initializing Data Matrix...</p>
    </div>
  </div>

  <!-- Main Content -->
  <div id="main-content">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
      <div class="container">
        <a class="navbar-brand" href="#">
          <i class="fas fa-cube"></i> OG
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link" href="#features">Features</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#how-it-works">How It Works</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#payor-analysis">Payor Analysis</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#contact">Contact</a>
            </li>
            <li class="nav-item">
              <a class="nav-link btn-login" href="#">Login</a>
            </li>
            <li class="nav-item">
              <a class="nav-link btn-signup" href="#">Sign Up</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
      <div class="container">
        <div class="row align-items-center">
          <div class="col-lg-6 hero-content">
            <h1>REIMAGINE DATA VALIDATION</h1>
            <p>OG is the next-generation platform for Excel validation, secure document storage, and advanced payor analysis. Experience the future of data management.</p>
            <div class="hero-btns">
              <a href="#" class="btn btn-primary">Activate System</a>
              <a href="#" class="btn btn-outline-primary">View Demo</a>
            </div>
          </div>
          <div class="col-lg-6 hero-image">
            <div class="hologram-container">
              <div class="hologram">
                <div class="hologram-layer"></div>
                <div class="hologram-layer"></div>
                <div class="hologram-layer"></div>
                <div class="hologram-content">
                  <h3 class="futuristic">DATA MATRIX ONLINE</h3>
                  <p class="mt-3">Advanced analytics engine initialized</p>
                  <div class="mt-4">
                    <div class="d-flex justify-content-around">
                      <div>
                        <h4 class="futuristic">98%</h4>
                        <small>Accuracy</small>
                      </div>
                      <div>
                        <h4 class="futuristic">2.4s</h4>
                        <small>Processing</small>
                      </div>
                      <div>
                        <h4 class="futuristic">∞</h4>
                        <small>Scalability</small>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
      <div class="container">
        <div class="section-title">
          <h2>ADVANCED CAPABILITIES</h2>
          <p>Powered by next-generation algorithms and neural processing</p>
        </div>
        <div class="row g-4">
          <div class="col-lg-4 col-md-6">
            <div class="feature-card">
              <div class="feature-icon">
                <i class="fas fa-check-double"></i>
              </div>
              <h3>Quantum Validation</h3>
              <p>Our quantum-inspired algorithms validate Excel files with unprecedented accuracy and speed, detecting even the most subtle data anomalies.</p>
            </div>
          </div>
          <div class="col-lg-4 col-md-6">
            <div class="feature-card">
              <div class="feature-icon">
                <i class="fas fa-cloud-upload-alt"></i>
              </div>
              <h3>Neural Storage</h3>
              <p>Secure document storage with neural network-based organization and retrieval systems for instant access to your data.</p>
            </div>
          </div>
          <div class="col-lg-4 col-md-6">
            <div class="feature-card">
              <div class="feature-icon">
                <i class="fas fa-chart-bar"></i>
              </div>
              <h3>Predictive Analysis</h3>
              <p>Advanced payor preference analysis with predictive modeling to forecast trends and optimize business strategies.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- How It Works -->
    <section class="how-it-works" id="how-it-works">
      <div class="container">
        <div class="section-title">
          <h2>SYSTEM OPERATION</h2>
          <p>Three-step process for maximum efficiency</p>
        </div>
        <div class="row g-4">
          <div class="col-lg-4">
            <div class="step-card">
              <div class="step-number">01</div>
              <h4>Data Upload</h4>
              <p>Securely upload your Excel files through our encrypted data channels.</p>
            </div>
          </div>
          <div class="col-lg-4">
            <div class="step-card">
              <div class="step-number">02</div>
              <h4>AI Processing</h4>
              <p>Our neural networks analyze and validate your data in real-time.</p>
            </div>
          </div>
          <div class="col-lg-4">
            <div class="step-card">
              <div class="step-number">03</div>
              <h4>Insight Delivery</h4>
              <p>Receive comprehensive reports and actionable insights.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Payor Analysis -->
    <section class="payor-analysis" id="payor-analysis">
      <div class="container">
        <div class="section-title">
          <h2>PAYOR INTELLIGENCE</h2>
          <p>Advanced analytics for insurance and payor data</p>
        </div>
        <div class="row g-4">
          <div class="col-lg-6">
            <div class="analysis-card">
              <div class="analysis-icon">
                <i class="fas fa-search-dollar"></i>
              </div>
              <h3>Pattern Recognition</h3>
              <p>Our AI identifies complex patterns in payor preferences and insurance data, providing insights that were previously undetectable.</p>
              <ul class="mt-3">
                <li>Advanced payment pattern analysis</li>
                <li>Provider preference mapping</li>
                <li>Reimbursement optimization</li>
              </ul>
            </div>
          </div>
          <div class="col-lg-6">
            <div class="analysis-card">
              <div class="analysis-icon">
                <i class="fas fa-shield-alt"></i>
              </div>
              <h3>Risk Assessment</h3>
              <p>Comprehensive analysis of insurance data with predictive risk modeling to safeguard your business decisions.</p>
              <ul class="mt-3">
                <li>Claims fraud detection</li>
                <li>Coverage gap analysis</li>
                <li>Future risk projection</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="container">
        <h2>READY TO ACTIVATE THE FUTURE?</h2>
        <p>Join the data revolution with OG's advanced validation and analysis platform. Experience the next generation of data management.</p>
        <a href="#" class="btn btn-light">Initialize Your Account</a>
      </div>
    </section>

    <!-- Footer -->
    <footer class="footer" id="contact">
      <div class="container">
        <div class="row">
          <div class="col-lg-4 mb-4">
            <h5><i class="fas fa-cube me-2"></i> OG</h5>
            <p>The future of data validation, storage, and analysis is here.</p>
            <div class="social-icons mt-3">
              <a href="#"><i class="fab fa-facebook-f"></i></a>
              <a href="#"><i class="fab fa-twitter"></i></a>
              <a href="#"><i class="fab fa-linkedin-in"></i></a>
              <a href="#"><i class="fab fa-instagram"></i></a>
            </div>
          </div>
          <div class="col-lg-2 col-md-6 mb-4">
            <h5>System</h5>
            <ul>
              <li><a href="#">Modules</a></li>
              <li><a href="#">Pricing</a></li>
              <li><a href="#">API</a></li>
              <li><a href="#">Updates</a></li>
            </ul>
          </div>
          <div class="col-lg-2 col-md-6 mb-4">
            <h5>Company</h5>
            <ul>
              <li><a href="#">About</a></li>
              <li><a href="#">Blog</a></li>
              <li><a href="#">Careers</a></li>
              <li><a href="#">Contact</a></li>
            </ul>
          </div>
          <div class="col-lg-2 col-md-6 mb-4">
            <h5>Support</h5>
            <ul>
              <li><a href="#">Help Center</a></li>
              <li><a href="#">Documentation</a></li>
              <li><a href="#">Community</a></li>
              <li><a href="#">Status</a></li>
            </ul>
          </div>
          <div class="col-lg-2 col-md-6 mb-4">
            <h5>Legal</h5>
            <ul>
              <li><a href="#">Privacy Policy</a></li>
              <li><a href="#">Terms of Service</a></li>
              <li><a href="#">Security</a></li>
              <li><a href="#">Compliance</a></li>
            </ul>
          </div>
        </div>
        <div class="footer-bottom">
          <p>&copy; 2023 OG - Original Group. All systems operational.</p>
        </div>
      </div>
    </footer>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Loading animation
    document.addEventListener('DOMContentLoaded', function() {
      const loadingScreen = document.getElementById('loading-screen');
      const loadingBar = document.getElementById('loading-bar');
      const mainContent = document.getElementById('main-content');
      
      let progress = 0;
      const interval = setInterval(() => {
        progress += Math.random() * 15;
        if (progress > 100) progress = 100;
        loadingBar.style.width = `${progress}%`;
        
        if (progress === 100) {
          clearInterval(interval);
          setTimeout(() => {
            loadingScreen.style.opacity = '0';
            loadingScreen.style.visibility = 'hidden';
            mainContent.style.opacity = '1';
          }, 800);
        }
      }, 300);
    });

    // Navbar background on scroll
    window.addEventListener('scroll', function() {
      if (window.scrollY > 50) {
        document.querySelector('.navbar').style.background = 'rgba(12, 15, 29, 0.95)';
        document.querySelector('.navbar').style.borderBottom = '1px solid rgba(0, 245, 255, 0.3)';
      } else {
        document.querySelector('.navbar').style.background = 'rgba(12, 15, 29, 0.8)';
        document.querySelector('.navbar').style.borderBottom = '1px solid rgba(0, 245, 255, 0.2)';
      }
    });

    // Smooth scrolling for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          window.scrollTo({
            top: target.offsetTop - 80,
            behavior: 'smooth'
          });
        }
      });
    });

    // Add futuristic cursor effect
    document.addEventListener('mousemove', function(e) {
      const cursor = document.createElement('div');
      cursor.classList.add('cursor-trail');
      cursor.style.left = e.pageX + 'px';
      cursor.style.top = e.pageY + 'px';
      cursor.style.position = 'absolute';
      cursor.style.width = '6px';
      cursor.style.height = '6px';
      cursor.style.background = 'var(--primary)';
      cursor.style.borderRadius = '50%';
      cursor.style.pointerEvents = 'none';
      cursor.style.zIndex = '9999';
      cursor.style.boxShadow = '0 0 10px var(--primary)';
      document.body.appendChild(cursor);
      
      setTimeout(() => {
        cursor.remove();
      }, 500);
    });
  </script>
</body>
</html>